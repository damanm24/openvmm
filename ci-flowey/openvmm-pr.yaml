##############################
# THIS FILE IS AUTOGENERATED #
#    DO NOT MANUALLY EDIT    #
##############################
trigger: none
pr:
  autoCancel: true
  drafts: false
  branches:
    include:
    - main
parameters:
- name: verbose
  displayName: Run with verbose output
  type: boolean
  default: false
resources:
  repositories:
  - repository: openvmm
    type: GitHub
    endpoint: microsoft
    name: microsoft/openvmm
jobs:
- job: job13
  displayName: Test GH Service Connection
  pool: HvLite-CI-Linux-Pool-CentralUS
  dependsOn: []
  condition: and(succeeded(), not(canceled()))
  variables:
  - name: FLOWEY_TEMP_DIR
    value: $(Build.StagingDirectory)/.flowey-internal
  steps:
  - checkout: openvmm
    fetchDepth: 0
    path: openvmm-github
    persistCredentials: true
  - bash: |
      (type -p wget >/dev/null || (sudo apt update && sudo apt install wget -y)) \
      && sudo mkdir -p -m 755 /etc/apt/keyrings \
      && out=$(mktemp) && wget -nv -O$out https://cli.github.com/packages/githubcli-archive-keyring.gpg \
      && cat $out | sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null \
      && sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg \
      && sudo mkdir -p -m 755 /etc/apt/sources.list.d \
      && echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
      && sudo apt update \
      && sudo apt install gh -y
      cd $(Pipeline.Workspace)/openvmm-github
      gh auth status
    condition: succeeded()
    displayName: Install GitHub CLI and verify authentication